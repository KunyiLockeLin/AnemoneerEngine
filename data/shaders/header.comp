#include "header.glsl"


layout( local_size_x = 1024) in;

layout( set = layoutSet_Model, binding = 20, rgba32f  ) uniform imageBuffer texelBuffer;

layout( set = layoutSet_Model, binding = 30 ) buffer outSSBO {
	int death[];
}outputData;