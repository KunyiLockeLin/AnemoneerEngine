#include "header.glsl"


layout( local_size_x = 1024) in;

layout( set = layoutSet_Compute, binding = 0, rgba32f  ) uniform imageBuffer texelBuffer;

layout( set = layoutSet_Compute, binding = 10 ) buffer outSSBO {
	int death[];
}outputData;