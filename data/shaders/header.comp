#include "header.glsl"


layout( local_size_x = 1024) in;

layout( set = layoutSet_Model, binding = Model_texel + 0, rgba32f  ) uniform imageBuffer texelBuffer;

layout( set = layoutSet_Model, binding = Model_storage + 0 ) buffer outSSBO {
	int death[];
}outputData;